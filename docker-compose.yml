version: '3.8'

services:
  postgres_db:
    image:  postgres:latest
    ports:
      - "5433:5432"
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: changemeinprod!